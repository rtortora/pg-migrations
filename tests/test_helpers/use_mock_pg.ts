import type { IMemoryDb, IBackup } from 'pg-mem';

export function useMockPg(): void {
/*  beforeEach(async ()=>{
    const { db, initialState } = require('../../__mocks__/pg') as { db: IMemoryDb, initialState: IBackup } ;
    (initialState as any).schemaVersion = (db as any).schemaVersion;
    initialState.restore();
  });
  */
}
